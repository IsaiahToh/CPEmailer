# Email Campaign Manager - Environment Configuration

# Email Service Configuration
# Options: gmail, outlook, yahoo, demo, custom
EMAIL_SERVICE=demo

# For Gmail, Outlook, Yahoo
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password

# For custom SMTP servers (if EMAIL_SERVICE=custom)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false

# Rate limiting (delay between emails in milliseconds)
EMAIL_DELAY=1000

# Server configuration
PORT=3000

# Instructions:
# 1. Copy this file to .env (it will be ignored by git)
# 2. Set EMAIL_SERVICE to your provider (gmail, outlook, yahoo, or custom)
# 3. Set EMAIL_USER to your email address
# 4. Set EMAIL_PASS to your app password (not your regular password)
# 5. For Gmail: Enable 2FA and generate an App Password
# 6. For custom SMTP: set SMTP_HOST, SMTP_PORT, and SMTP_SECURE
